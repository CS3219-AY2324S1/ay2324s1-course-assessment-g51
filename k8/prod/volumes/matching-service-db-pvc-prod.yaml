apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: matching-service-db-pvc-prod
    namespace: prod
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: matching-service-storage-prod
    resources:
        requests:
            storage: 200Mi
        limits:
            storage: 1Gi
status: {}
