apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgres-pvc-prod
    namespace: prod
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: postgres-storage-prod
    resources:
        requests:
            storage: 200Mi
        limits:
            storage: 1Gi
status: {}
